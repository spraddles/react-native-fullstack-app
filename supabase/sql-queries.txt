/* 
*   Run these queries in the supabase SQL editor.
*   This will now let you run 'supabase.rpc()' 
*   remote procedure calls. Example below:
*
*   const response = await supabase.rpc('get_user_by_email', { 
*       email_param: 'some@email.com 
*   })
*/


CREATE OR REPLACE FUNCTION get_user_by_email(email_param TEXT)
RETURNS TABLE (email text, created_at timestamptz) 
LANGUAGE SQL
SECURITY DEFINER
SET search_path = public
AS $$
    SELECT email, created_at 
    FROM auth.users 
    WHERE email = email_param;
$$;

GRANT EXECUTE ON FUNCTION get_user_by_email TO authenticated;
GRANT EXECUTE ON FUNCTION get_user_by_email TO service_role;

